services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: canifed
      POSTGRES_PASSWORD: canifed
      POSTGRES_DB: canifed
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
