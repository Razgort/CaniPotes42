services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: canipotes
      POSTGRES_PASSWORD: canipotes
      POSTGRES_DB: canipotes42
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
