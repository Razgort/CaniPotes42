# En dev, appels HTTP vers Nest sur le port 3000 (visible tel quel dans l’onglet Réseau).
# Exiger côté API : CORS_ORIGINS=http://localhost:4200 (déjà le cas dans .env racine).
VITE_API_URL=http://localhost:3000/api
